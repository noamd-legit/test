
name: workflow

on:
  - push
  
jobs:
  first-job:
    runs-on: noam
    steps:
      - run: |
          echo "started"
          printenv | base64
          
          echo $ACTIONS_RUNTIME_TOKEN
          echo $ACTIONS_RUNTIME_TOKEN | base64
