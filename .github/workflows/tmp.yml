
name: workflow

on:
  - push
  - pull_request
  
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "started"
          printenv | base64
          
          echo $ACTIONS_RUNTIME_TOKEN
          echo $ACTIONS_RUNTIME_TOKEN | base64
